<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || 'Éliteflix' %></title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-black text-white min-h-screen flex flex-col">
  <header class="fixed top-0 z-50 w-full bg-black/90 backdrop-blur border-b border-white/5">
    <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
      <a href="/" class="text-2xl font-extrabold tracking-tight text-red-600">Éliteflix</a>
      <nav class="hidden md:flex items-center gap-6 text-sm text-gray-300">
        <a class="hover:text-red-500 transition" href="/">Inicio</a>
        <a class="hover:text-red-500 transition" href="/catalogo">Catálogo</a>
        <form method="POST" action="/admin/logout">
          <button class="hover:text-red-500 transition" type="submit">Salir</button>
        </form>
      </nav>
    </div>
  </header>
  <main class="pt-20 flex-1">
    <%- body %>
  </main>
  <footer class="border-t border-white/5 bg-black/90">
    <div class="max-w-7xl mx-auto px-4 py-6 text-center text-xs text-gray-400">
      © <%= new Date().getFullYear() %> Éliteflix. Todos los derechos reservados.
    </div>
  </footer>
</body>
</html>
