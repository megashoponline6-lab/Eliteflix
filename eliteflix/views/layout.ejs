<header class="fixed top-0 z-50 w-full bg-black/90 backdrop-blur border-b border-white/5">
  <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
    <a href="/" class="text-2xl font-extrabold tracking-tight text-red-600">Éliteflix</a>

    <!-- 🔥 Menú actualizado -->
    <nav class="flex items-center gap-4">
      <a href="/" class="text-slate-200 hover:text-white">Inicio</a>
      <a href="/catalogo" class="text-slate-200 hover:text-white">Catálogo</a>

      <% if (session && session.client) { %>
        <a href="/perfil" class="text-slate-200 hover:text-white">Mi perfil</a>
        <form method="POST" action="/salir" class="inline">
          <button class="text-slate-200 hover:text-white">Salir</button>
        </form>
      <% } else { %>
        <a href="/inicio" class="text-slate-200 hover:text-white">Iniciar sesión</a>
        <a href="/registro" class="text-slate-200 hover:text-white">Registrarme</a>
      <% } %>

      <a href="/admin/login" class="text-slate-400 hover:text-white">Admin</a>
    </nav>
  </div>
</header>
